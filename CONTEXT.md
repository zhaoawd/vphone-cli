# vphone-cli 领域术语

## 固件与创建

**固件组合**：iPhone 固件、cloudOS 固件、变体和选项的确定组合。验证结果只适用于证据记录的组合。

**补丁实验记录**：一次补丁运行的输入条件、执行结果、产物和证据记录。失败的运行也具有实验记录。

**创建检查点**：一次 VM 创建过程中已确认阶段结果的持久记录。检查点不证明其引用的产物此后未发生变化。

**固件恢复（restore）**：将固件写入指定 VM 的恢复流程。

**创建续跑（resume）**：重新验证既有创建结果后，从允许继续的阶段执行。创建续跑不等于重复执行固件恢复。

## 相机

**generation**：调用方标记相机源归属的名称。相同名称可用于多次呈现。

**呈现标识（presentation_id）**：宿主为一次相机呈现生成的独立标识。回执使用它区分相同 generation 下的新旧内容。

**相机复制回执**：客户机确认已从共享内存复制指定呈现的一帧。它不表示应用已经显示或识别该帧。
