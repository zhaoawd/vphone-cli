# A3：rig2 触控运行准备与回归

日期：2026-09-16。A3 进行中；不新增完整支持组合。

## M0 收口

项目 `.venv` 缺失，已用 `make setup_venv` 按锁文件重建，依赖能力检查通过。Python 113 项通过；XCTest 139 项、3 项跳过、0 失败；Swift Testing 333 项、53 个 suite 通过。Swift 首次沙箱内运行报告 9 个问题，同源码沙箱外复跑通过。

定位启动修复和 E5 记录提交为 `e2dcd53`。相机、历史清理、最新工作项状态和 less 输入参数提交为 `c0c0bdf`。C4 仍暂停，完成计数仍为 17/28。

## 实验设置

使用 `vm-2607-rig2`，26.1 / 23B85 EXP；当前 `.build/vphone-cli.app` 的帮助命令执行通过。启动时持有 autophone rig2 任务锁，启动前检查 VM 目录锁、控制 socket 和固件事务。宿主启动使用明确不存在的 `--vphoned-bin` 路径，保留原客户机。每轮结束向本轮创建的宿主进程发送 SIGINT，等待退出并记录 socket 清理结果。

主 VM PID 21605、健康监控 PID 39330、agent PID 81563 在实验前已核对；本轮未向其控制接口发送请求。

## 运行结果

第一次连接成功后立即截图为黑屏，上滑后出现锁屏。该轮 UI 尚未稳定，不用于触控失败判定。

第二次客户机连接后等待 10 秒，截图显示首次设置欢迎页。执行 `(645,2790) → (645,1600)`、300 ms 的上滑后进入语言选择页；第二次同参数上滑使语言列表滚动。客户机声明 `touch`，现有路由选择客户机注入路径。Home 键后仍为语言选择页。

9 月 15 日欢迎页上滑后画面不变的现象本轮未复现，原因未查明。当前仍处首次设置流程，不能由上述结果推断应用内底部回主屏、长按、缩放或相机验收通过。

两轮宿主均退出 0，退出后 socket 不存在，日志出现 `Guest stopped`。当前 rig2 已关闭。

## 证据与下一步

本地 `research/artifacts/a3-rig2-2026-09-16/requests.jsonl` 保留两轮请求。`boot.log`、`before.jpg`、`after.jpg`、`second-after.jpg`、`home-after.jpg` 和 `exit.json` 为第二轮结果；第一轮同名输出已被覆盖，不能作为独立归档引用。探测脚本 `research/a3_rig2_probe.py` 已改为每轮使用带时间的目录，避免后续覆盖。

新增路由回归覆盖：孤立 move/up 不启动手势；未观察到断线事件但连接代次已变化时丢弃旧手势；断线后新手势使用原生路径。该测试验证路由状态，不替代客户机实际释放触点或原生输入验收。

`swift test --disable-sandbox --cache-path .build/test-cache --filter TouchRouteTests` 在沙箱外执行，5 项通过。首次专项因默认模块缓存无写权限未能编译。探测脚本语法及 `git diff --check` 通过。完整回归日志已复制到上述证据目录；本轮重建依赖与测试产物后，可用空间约 43 GiB，未删除固件或 VM。

下一步需完成 rig2 首次设置，再建立设置应用内底部上滑的可重复判据，继续 A3 和 E6。已询问首次设置采用简体中文、中国大陆并暂不登录 Apple 账号；答复前不继续设置步骤。
