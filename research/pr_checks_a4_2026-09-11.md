# A4：PR 快速测试接入

日期：2026-09-11。状态：A4 快速检查接入与远端正常/失败反馈验收完成。按用户要求暂停 C4 完整 less 验收，保留已有实现与未完成状态。

## 快速检查

`.github/workflows/checks.yml` 在 push、pull_request 和手动触发时运行，使用 `macos-26` ARM64。该标签的架构见 [GitHub runner 文档](https://docs.github.com/en/actions/reference/runners/github-hosted-runners)。只初始化 Swift vendor 子模块，不下载固件、CFW 资源或创建 VM。

明确安装 Python 3.13、Keystone、zstd、xz 和 actionlint，调用既有 `make setup_venv` 与 A1 的 `make test`。在干净 checkout 中生成测试编译所需的 `VPhoneBuildInfo.swift`，无需运行签名构建。脚本检查使用 Python AST 和 bash/zsh 的 `-n`，只验证语法，不执行脚本；这不等同于完整语义静态分析。

pip 缓存随 Python 版本及 requirements/setup 脚本变化。Swift 缓存键包含操作系统、架构、Xcode/Swift/Python/原生依赖版本、子模块提交、Package 输入和当前提交；仅在相同工具链及依赖身份内复用先前构建。requirements 尚未锁定完整依赖版本，D2 仍未完成。

失败日志经 `tee` 保存，`pipefail` 保留失败退出状态。无 `continue-on-error`；摘要和日志上传使用 `always()`，日志保留 7 天。仓库只读权限，checkout 不保留凭据。

## 固件检查隔离

`.github/workflows/firmware-checks.yml` 是独立手动入口，仅允许 main，且要求仓库变量 `ENABLE_FIRMWARE_CI=true`。需另行配置 `vphone-firmware` 标签的 macOS ARM64 专用运行器、`firmware-validation` 环境（建议要求审批）、Swift 工具链及 `VPHONE_TEST_FIXTURES` 的完整只读样本路径。此工作没有注册运行器、设置仓库变量或触发该任务。

手动任务只调用 A1 `make test_firmware`，预检缺失或空文件会失败；不启动 VM，也不执行完整 less 验收。私有 VM 的启动和端到端验收尚无自动任务，不因增加此入口而标记完成。

`run_tests.py` 在启动 Swift 测试前清除继承的 `VPHONE_TEST_*`、`VPHONE_LESS_*`、`VPHONE_C4_*` 选择变量；固件模式只重新设置显式的 fixture 路径。这样快速检查不会因个人终端遗留的验收变量而处理真实固件。测试内部启动崩溃子进程所需变量仍由测试自身传入。

## 验证与剩余工作

- actionlint 1.7.12 校验所有工作流通过；专用运行器标签在 `.github/actionlint.yaml` 中显式声明。
- 定向测试 9 项通过，覆盖 Python/shell 故意语法失败、检查不执行脚本、测试进程退出码透传、fixture 预检和环境隔离。
- `make test` 通过：Python 81 项；XCTest 80 项（3 项按条件跳过）无失败；Swift Testing 331 项通过。本轮没有重新下载固件或运行镜像验收。
- 实际失败探针：临时增加 `test_a4_deliberate_failure.py`，执行与 CI 相同的 `make test | tee` 管道（启用 pipefail）。Python 共执行 82 项，仅故意断言失败，make/管道退出码为 2；错误保留在日志，Swift 阶段没有启动。探针随后删除，未纳入提交。
- 本地日志：`/tmp/a4-fast.log`、`/tmp/a4-deliberate-failure.log`。这些本地结果不替代 GitHub 托管执行和 PR 状态验证。
- 已完成 GitHub 首次无缓存运行、缓存恢复、正常 PR、预期失败 PR 和移除断言后的成功运行，结果见下表。
- GitHub 分支保护是否要求该检查尚未配置；工作流失败状态本身不等于已经禁止合并。A4 的发布包验证仍依赖 D1。

## GitHub 实际验收（2026-09-11）

代码 `c7f841f` 已推送至集成分支。临时 [PR #1](https://github.com/zhaoawd/vphone-cli/pull/1) 以集成分支为 base，只用于检查反馈，不将其相对 main 的 39 个提交纳入本次审查。

| 场景 | 运行 | 结果 |
| --- | --- | --- |
| 首次无缓存 push | [34569452212](https://github.com/zhaoawd/vphone-cli/actions/runs/34569452212) | 成功，建立 pip 与 Swift 缓存 |
| 正常文档变更 PR | [34569615216](https://github.com/zhaoawd/vphone-cli/actions/runs/34569615216) | 成功 |
| 临时失败断言 PR | [34570090866](https://github.com/zhaoawd/vphone-cli/actions/runs/34570090866) | 预期失败；82 项 Python 测试只有该断言失败，PR 显示 FAILURE，日志上传成功 |
| 移除断言后的 PR | [34570276128](https://github.com/zhaoawd/vphone-cli/actions/runs/34570276128) | 成功，pip/Swift 缓存恢复，完整快速测试通过 |

托管环境实际为 macOS 26.6.2 ARM64、Xcode 26.6、Swift 6.3.3、Python 3.13.15。首次成功运行通过 Python 81 项、XCTest 80 项（3 项按条件跳过）及 Swift Testing 331 项。未初始化 CFW 资源或工具源码子模块，没有个人固件目录。

故意失败运行同时确认 pip cache 命中、Swift restore-key 命中。运行失败后，摘要步骤和日志上传仍成功；日志归档名为 `fast-check-logs-34570090866-1`。三次临时分支的重复 push 检查主动取消，真正的 pull_request 检查保留；取消状态不属于故意断言的失败证据。

最终成功运行再次通过 Python 81 项、XCTest 80 项（3 项按条件跳过）和 Swift Testing 331 项。完整日志与 SHA-256 清单保存于本地 `research/artifacts/a4-ci-2026-09-11/`，大小约 1.8 MiB；GitHub 上传日志保留期仍为 7 天。

A4 完成范围为快速检查接入、测试隔离、依赖准备、缓存身份、摘要/日志和实际 PR 失败反馈。专用固件运行器未启用、私有 VM 验收未执行、分支保护未修改、D1 发布包验证仍待完成。C4 完整 less 验收继续按用户要求暂停。

临时 PR #1 已关闭且未合并；本轮临时工作树及本地、远端 `codex/a4-ci-validation` 分支已删除。故意失败断言未进入集成分支。
