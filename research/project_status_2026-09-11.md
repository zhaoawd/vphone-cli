# 项目、分支、功能进展与待办

最新状态见 [2026-09-12 更新](project_status_2026-09-12.md)。以下保留 9 月 11 日调查及后续记录。

更新日期：2026-09-11。代码基线：`codex/autophone-location-multivm-integration`，`da678a3`。

同日后续执行：已移植并验证定位修复分支的 `82dae74`、`fb57473`，提交为 `9682b65`，保留当前 VPhoneCore 模块边界；signcert 差异审计确认当前行为已覆盖，无需移植。C4 已实现暂存和显式恢复，四个非 less 真实场景及实际 less 中断恢复通过；完整 less 成功路径因本轮磁盘空间不足仍待验收；提交后正式 CLI 冒烟还被当前宿主的私有 entitlements 策略拒绝，移除私有 entitlements 的临时副本仅通过恢复逻辑验证。详见[定位修复整合](location_fixes_integration_2026-09-11.md)与[C4 实现与验收](firmware_recovery_c4_2026-09-11.md)。E5 仍需真实 VM 验收，A4 的 GitHub 正常/失败反馈验收已完成，E1 的消息边界、容量、权限和读写期限已完成；原清单完成数量为 11/28。以下分支表及“本次未测试”描述保留首次状态调查的基线，后续验证以整合记录为准。

本次已执行 `git fetch origin`，检查分支包含关系、补丁等价性、源码和研究记录。检查开始时工作区干净。本次只更新文档，没有重新执行构建、测试、固件处理或 VM 验收；下述测试结果注明为 2026-09-10 既有记录。未读取或更新 `TODO.md`。

## 整体情况

项目已具备 VM 创建、固件处理、恢复、启动、库管理、客户机控制、定位和虚拟相机实现。近期完成的主要工作是 VM 操作互斥、停止目标校验，以及固件补丁结构化结果和产物验收。

原有 28 项迭代工作中，11 项完成：A1、A2、A4、B1–B4、C1–C3、E1；17 项未完成。该数量是工作项计数，不能换算为项目功能完成百分比。未完成项中包含已有实现但尚未完成验收的触控、定位和相机能力。

当前限制主要在三处：固件事务已实现，但完整 less 成功路径尚未完成验收；宿主机控制接口仍依赖 GUI；完整运行和双 VM 能力缺少当前版本的系统验收。补丁产物通过不能代替恢复、启动或应用内功能通过。

依据：[原始清单](project_iteration_checklist_2026-09-08.md)、[C3 完成验收](c3_completion_acceptance_2026-09-10.md)、[合并复验](c3_remote_merge_2026-09-10.md)。

## 分支情况

下表“独有提交”按 Git 提交图相对当前 HEAD 计算，不等于功能缺失数量。经过 cherry-pick、重构或冲突整理的实现可能具有不同提交 ID。

| 分支 | 当前情况 | 原因、影响与建议 |
| --- | --- | --- |
| `codex/autophone-location-multivm-integration` | 当前工作分支，`da678a3`；与对应 origin 分支同步 | 包含最新 C3 合并复验，是本报告的代码基线 |
| `origin/main`、`origin/autophone-location-multivm-integration` | 均为 `06370de`，均为当前 HEAD 的祖先；当前分支领先 34 个提交 | 最近操作保护、触控修复及 C1–C3 工作尚未进入远端 main；建议明确主线合并范围，并保留运行验收限制 |
| 本地 `main` | `d163eed`；落后 `origin/main` 90 个提交，落后当前 HEAD 124 个提交 | 本地 main 不能用于判断最新开发进展；需要使用 main 时再同步 |
| `codex/upstream-c9ad3c7-integration` | `06ebae9`，当前 HEAD 已包含全部提交 | iOS 27 历史集成阶段已进入当前分支；无需按分支名再次合并 |
| 本地 `codex/vphone-core-integration` | `60f9bbb`，当前 HEAD 已包含全部提交；本地落后其 origin 分支 1 个提交 | 核心提取和历史固件适配已集成 |
| `origin/codex/vphone-core-integration` | `c22d179`，相对当前 HEAD 有 1 个独有提交 | 提交涉及 signcert 资源布局；当前历史已有同主题 `4dcbdb8`。`git cherry` 未判为相同补丁，需核对具体差异，不能按标题认定重复或直接合并 |
| `codex/app-knowledge-compiler`、`codex/location-system-runtime` | 均为 `baccfc2`；该提交的补丁已在当前 HEAD 中存在等价版本 | 分支名称不证明实现了应用知识编译能力；该 HEAD 实际为持久定位功能。`location-system-runtime` 跟踪的是 `origin/main`，跟踪配置也应复核 |
| `codex/app-knowledge-compiler-fixes` | `fb57473`；相对 HEAD 有 3 个独有提交，其中基础功能 `baccfc2` 补丁等价，另 2 项不等价 | `82dae74` 是定位流加固，`fb57473` 是并发测试修正。应优先逐项比对当前控制器、客户机和测试，确认哪些行为仍需移植 |
| `codex/location-capability-consolidation` | `0fc1173`；相对 HEAD 有 1 个独有提交，未检测到等价补丁 | 当前已有 `VPhoneCore` 定位控制器；需按行为核对差异，不能依据 Git 独有提交断言功能缺失 |
| `merge/upstream-2026-07` | `288b56f`，全部提交已被当前 HEAD 包含 | 属于已集成历史分支；确认不再使用后可另行整理 |
| `origin/merge/upstream-2026-07-b` | 相对 HEAD 有 1 个独有提交 | 历史合并分支，尚未逐项核对内容，不认定为待移植功能 |
| `origin/feat/vphoned-modules-and-menus`、`origin/swift-yyds` | 相对 HEAD 分别有 3、38 个独有提交 | 尚未做语义差异审计；不得据提交数直接合并或宣称已全部覆盖 |
| 缓存的 `upstream/main` | `339719f`，当前 HEAD 已包含 | 当前 Git 只配置 origin 远端；这只是保留的远端跟踪引用，不能代表上游当前最新状态 |

分支待办独立于原有 28 项清单：定位修复差异审计、资源布局差异审计、主线合并安排，以及完成核对后的历史分支整理。本次没有合并、删除或推送分支。

## 具体功能进展

| 功能 | 当前进展 | 未完成范围及原因 |
| --- | --- | --- |
| 测试入口 A1 | 已完成统一快速测试入口，区分无固件测试和固件环境测试 | A4 已完成 GitHub 快速检查及正常/失败反馈验收；专用固件运行器、VM 验收和 D1 发布包检查仍需各自推进 |
| SystemOS/AEA 缓存 A2 | 已完成共享识别、校验与原子发布；覆盖 AEA、已解密 DMG、无效输入和失败路径 | 历史镜像及替身验证不覆盖所有真实固件安装组合 |
| CFW 挂载 B1 | 已完成任务挂载隔离和失败清理；原生 APFS 测试有记录 | 完整 CFW 安装和跨变体阶段迁移仍需各自验收 |
| VM 生命周期 B2–B4 | 已完成共享锁、离线操作保护、停止目标身份校验及停止结果确认 | B3 在 iOS 26.6.1 EXP 上有五类实测；DFU、应用包内二进制及部分失败分支未完成运行验证。锁实现通过不能替代双 VM 全流程验收 |
| 兼容性 C1 | 已有机器可读清单，分开记录代码可选择、补丁验证和能力验证 | 历史条目保留原日期和范围；未知组合不升级为支持 |
| 补丁结果 C2–C3 | 已完成结构化状态、必要性判定、消融及全部补丁器迁移；必要项失败会使组件失败 | 解决了部分补丁成功掩盖必要项失败的问题；C4 已实现多组件暂存与显式恢复；完整 less 成功路径仍未验收 |
| less | 默认 26.1 组合完整流水线及独立产物验证通过 | 尚未由本轮验证恢复、启动及非默认安装选项；未执行两次完整镜像逐字节比较 |
| GUI 触控 A3 | 客户机触控优先策略已提交；26.6.1 EXP 的点按、拖动、顶部手势有验证 | 底部上滑回主屏在客户机 HID 和原生 VZ 两条路径均失败，原因未查明；旧版、27.x、断线释放等完整矩阵未完成 |
| 自动化控制 E1–E4 | 已有 Unix socket、JSON 命令、vsock 客户机控制及重连机制 | E1 已统一为 2 MiB 消息上限、5 秒读写期限和 16 个并发连接；socket 创建仍位于 GUI 分支，headless 控制未完成 |
| 系统定位 E5 | 已有固定源、流、owner、generation/sequence、暂停和重连处理，以及固定位置持久化 | 真实 socket、宿主重启、损坏状态和双 VM 隔离验收未完成；定位修复已通过 `9682b65` 整合。固定位置持久化不能写成整个流历史持久化 |
| 虚拟相机 E6 | 已有图像/视频输入、独立传输和 generation 回执 | 回执注释要求帧索引匹配，代码仅要求同 generation 且两个索引大于零；需先定义成功语义，再验证应用显示与 QR 识别 |
| 构建和分发 D1–D2 | `make build` 构建签名通过有记录；已有应用包资源布局修复 | 开发构建与完整发布构建尚未统一，依赖解析尚未完整固定；不能据开发包构建成功认定完整包可移植 |
| 创建和恢复 D4–D5 | 已有创建编排和各阶段入口 | 尚无贯穿 prepare、patch、restore、CFW、first-boot 的持久检查点、续跑及统一诊断 |
| 多 VM F2 | 已有 VM 库、每 VM 状态/socket 及跨入口互斥实现 | 两台 VM 同时操作、停止一台不影响另一台、定位/相机归属的完整运行验证未完成 |
| 性能 F3 | 尚无该清单要求的固定实验基线 | 无当前命令延迟、长时内存增长、相机丢帧测量依据，暂不作性能结论 |

代码依据：[HostControl](../sources/vphone-cli/VPhoneHostControl.swift)、[AppDelegate](../sources/vphone-cli/VPhoneAppDelegate.swift)、[定位控制器](../sources/VPhoneCore/VPhoneSystemLocationController.swift)。运行依据：[评审及触控/停止验证](review_fixes_2026-09-08.md)、[离线操作保护](offline_op_guard_2026-09-09.md)。

## 最新验证证据及限制

2026-09-10 合并复验记录：`make test` 退出码 0，Python 73 项通过；XCTest 21 项中跳过 1 项、其余 20 项通过；Swift Testing 314 项通过。`make build` 构建和签名通过。这是已记录的验证结果，本次未重跑。

| iPhone 输入 | cloudOS 输入 | 已通过的 C3 场景 |
| --- | --- | --- |
| 26.1 / 23B85 | 26.1 / 23B85 | regular、dev、jb、exp；另有 less 默认场景 |
| 26.3 / 23D127 | 26.3 / 23D129 | regular、dev、jb、exp |
| 26.6.1 / 23G83 | 26.4 / 23E5207q | regular、dev、jb、exp、jb-frida、exp-frida |
| 27.0 / 24A5408d | 26.4 / 23E5207q | regular、dev、jb、exp、jb-frida、exp-frida |
| 18.6.2 / 22G100 | 26.1 / 23B85 | regular、dev、jb、exp |

24 个非 less 场景已完成必要集合、落盘检查和迁移前后完整 payload/records 比较。合并后又逐组件比较了当前输出与合并前 CLI 实际产物，全部通过。less 完成真实字节链、21 组件摘要、独立解密及 root hash 校验；合并未更改 Filesystem 实现，仅复验相关真实产物比较，没有再次构建 less 镜像。

上述结果限定为固定输入、选项与 AVPBooter 来源。Frida 开关场景通过表示补丁产物通过，不表示 Frida 客户端 instrumentation 已验收。26.3 本轮 cloudOS `23D129` 不覆盖历史 `23D128`。大型原始产物与日志被 Git 忽略，其他 checkout 不保证具备；本次未逐份重查这些产物。

## 原清单剩余 17 项与原因

| 工作项 | 待办 | 为什么仍需执行 |
| --- | --- | --- |
| A3 | 完成触控版本/连接矩阵，调查底部回主屏失败 | 已有明确失败现象，且旧版与 27.x、断线释放等证据未齐 |
| C4 | 补齐完整 less 成功路径及最终验收 | 暂存、提交记录、故障注入和真实中断恢复已实现并验证；本轮可用空间降至约 2.7 GiB 后主动停止镜像验收 |
| C5 | 统一输出可重现的实验记录 | 已有报告和局部验收脚本，尚未统一保存每次运行的工具、输入、选项、失败阶段等完整条件；依赖 C4 |
| D1 | 统一构建及完整包资源验证 | 开发包和发布包资源范围不同 |
| D2 | 固定和核验依赖解析 | 新环境尚不能仅凭现有依赖声明重建已验证组合；依赖 D1 |
| D3 | 提取共同 CFW 安装步骤 | AEA 缓存已有共享逻辑，其他共同阶段和各变体阶段序列仍需整理与验证 |
| D4 | 创建检查点与续跑 | 当前中断后没有经验证的完整恢复路径；依赖 B4、C4、D3 |
| D5 | 统一只读诊断输出 | 环境、资源、占用、创建阶段和失败原因分散；依赖 C5、D2、D4 等状态基础 |
| E2 | 分离传输、命令和画面能力 | 控制接口依赖具体 VM 视图，限制 headless；依赖 E1 |
| E3 | 接入 headless 控制 socket | 当前 socket 生命周期在 GUI 分支；依赖 E2 |
| E4 | 客户机传输故障和取消测试 | 现有超时/重连实现尚无完整契约验收，需区分调用超时与客户机实际停止 |
| E5 | 定位真实入口及重启/隔离验证 | 控制器单元测试不能代替真实通道与双实例结果；依赖 E3、E4 |
| E6 | 定义相机回执并验证应用结果 | 发布帧、客户机消费帧、应用显示和识别是不同结果；依赖 E3、E4 |
| F1 | 精确组合端到端验收 | C3 不覆盖恢复、两次启动、交互和客户端能力；完整验收依赖 A3、D4、E5、E6 |
| F2 | 两 VM 并行完整流程 | 每 VM 锁与状态分离的实现不能独立证明运行中无串用或相互清理 |
| F3 | 性能及长时运行基线 | 缺少固定环境和负载测量；应在 F1、F2 后取得可比较结果 |
| F4 | 文档与交付核对 | 旧模块图、四变体表、固定补丁数及历史状态与当前五变体/方法/记录口径并存 |

## 当前执行调整（2026-09-11）

用户因磁盘空间不足决定先绕过 C4，暂停完整 less 成功路径验收，保留 C4 未完成状态。A4 已完成无缓存 push、正常 PR、预期失败及移除失败样本后的成功检查，并验证日志上传和缓存恢复。A4 完成时数量为 10/28；本轮 E1 完成后为 11/28。详见 [A4 接入记录](pr_checks_a4_2026-09-11.md)。

C4 继续暂停。E1 已完成，下一项推进 E2 的传输、命令与画面能力分离。原因：E3 的 headless 控制需要解除具体 VM 视图依赖；E2 也需处理尚无统一取消机制的命令回调。

## 建议执行顺序（初始建议）

1. 先完成定位修复分支差异审计，并整理当前集成分支进入 main 的具体范围。原因：未对齐的修复会影响 E5 设计和测试基线；远端 main 尚不含近期 34 个提交。此建议不表示应跳过审查直接合并。
2. 补齐 C4 的完整 less 验收，同时安排 A4 与 A3 的独立验证。原因：C4 的 C3/B4 前置条件已经完成；A4 建立回归检查；A3 仍有已复现的交互失败。
3. 按 E1 → E2 → E3/E4 → E5/E6 推进自动化。原因：协议和画面耦合是 headless、定位及相机完整验收的前置问题。
4. 按 D1 → D2、D3 → D4 推进分发和创建恢复；C4 后完成 C5，并汇入 D5。原因：完整包、稳定输入和可识别阶段是新环境重现与中断恢复的基础。
5. 汇总 F1/F2，再执行 F3 和最终 F4。当前可提前开展限定组合的启动冒烟，但不能因此将完整 F1 标记完成。原因：完整运行矩阵依赖尚未完成的恢复和自动化能力。

早期文档中的“C3 仍进行中”、26.4 匹配失败和 less 工具/空间阻塞已被后续修正及验收更新。其历史正文保留；最新状态应以本报告链接的完成记录为准。底部回主屏失败仍未查明原因，不能将旧待验证假设写成当前原因。
