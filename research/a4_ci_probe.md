# A4 CI validation probe

This temporary branch verifies pull-request checks against the integration branch.
The initial revision changes documentation only. A later revision will introduce
one deliberate assertion failure, then remove it. This PR is not intended to merge.
