# rig2 首次设置进展

日期：2026-09-16。用户指定简体中文、中国大陆、暂不登录 Apple 账号，以继续 A3/E6 应用验收。

后续的服务恢复、视频 EOF 局部验证和 QR 解码对照见 [E6 后续记录](camera_e6_followup_2026-09-16.md)。

已通过 UI 选择简体中文、中国大陆，保留默认显示大小，选择“不通过另一台设备设置”，使用推荐的简体拼音、English (US) 和表情符号键盘，继续通过“数据与隐私”说明页。

此前页面要求选择使用者年龄段：儿童（13 岁及以下）、青少年（14–17 岁）、成人（年满 18 岁）。用户随后手动完成剩余选择并进入桌面。本任务复核桌面及设置应用可见；设置应用的 Apple 账号区域仍显示登录提示。用户手动选择的具体其他选项未记录，不自行补全。

第一次会话的宿主 PID 36898 正常退出，socket 已清理。该轮验收脚本的子进程继承终端输入，部分 JSON 被客户机串口 Shell 接收并返回命令不存在；脚本随后将子进程 stdin 设为 DEVNULL，控制请求独立发送。该问题不表示生产触控实现有相同缺陷。

第二次会话持有 rig2 任务锁，宿主 PID 38606，保留原客户机。唤醒后连续上滑进入语言选择页，语言和地区应用完成后进入后续设置。欢迎页不响应上滑的原因尚未查明，不能仅由唤醒后成功确定其原因。

第二轮证据位于 `research/artifacts/rig2-setup-20260916-162549/`，包含请求记录、启动日志、语言/地区及当前年龄段页面截图。第一轮证据位于 `research/artifacts/rig2-setup-20260916-162214/`。本轮未操作主实例控制入口，未修改生产代码、固件补丁或账号信息。

## 用户完成设置后的应用验收

设置应用成功启动，PID 4482。底部 `(645,2790) → (645,1600)`、300 ms 上滑后仍停留在设置，历史应用内回主屏失败再次出现。该次结果尚未排除显示休眠影响，A3 保持进行中。

客户机与消费端原文件已备份到第二轮证据目录。SHA-256 分别为 `2b90b8e555c86e794733e661e9bdee4dae510e7f5fcc0ef0e9396f673a168cf4` 和 `ffd0401e31522d0a7b2aaad37aea011489f2cd875d3ee15da5e000ba4506c9c4`。部署此前隔离构建的配套版本后，客户机声明 `vcam_receipt_v3`。相机应用在唤醒并返回主屏后启动，PID 5128；两张截图分别显示红色 `test-a` 与蓝色 `test-b`。

两次 `camera_present` 均未取得复制回执，观察记录文件不存在；消费端日志的最后记录来自此前 PID 1208，而本轮进程为 4869。实际加载版本尚未确认，不把图像显示作为新版复制回执通过。QR 请求后的截图仍为蓝色图像，未验证二维码显示或识别。已停止相机源并正常关闭 PID 38606，退出码 0、socket 已删除，准备完整重启后复验。

## 完整重启后的 E6 结果

第三轮证据目录为 `research/artifacts/rig2-setup-20260916-164640/`，宿主 PID 42048。TweakLoader 明确报告消费端没有 cdhash、拒绝加载。此前使用的隔离构建产物未签名；按项目构建流程使用 `ldid -S` 签名后重新部署，加载日志确认成功，观察记录文件出现。签名产物 SHA-256 为 `314afa7e8f8339646d816f1bf90bb0bd0e6fd41e3fd5f2cf0539d4d466466913`。这确认了本轮验收产物的签名问题，不表示正式安装脚本缺少签名步骤。

| 实验设置 | 结果 |
| --- | --- |
| 红色图像、同 generation 切换蓝色图像 | 两次均获得发布与消费回执；presentation_id 改变；相机截图显示蓝色 test-b |
| 使用第一次 presentation_id 停止第二次呈现 | 拒绝，返回 presentation does not own the camera source |
| neutral 停止策略 | 生成新 generation 与 presentation_id，取得回执，相机截图显示白色 |
| QR 呈现 | 取得回执，相机截图显示二维码；未确认解码 |
| keep_last | streaming=false、guest_frame_cleared=false，随后状态无有效回执 |

系统 BarcodeScanner 初次启动崩溃，异常调用栈包含 `_setActiveFormat:resetVideoZoomFactorAndMinMaxFrameDurations:sessionPreset:`。临时在 libcamfix 的 Executables 过滤器加入 BarcodeScanner 后，日志确认加载 libcamfix、将 nil activeFormat 替换为支持格式，应用启动并显示二维码。未观察到解码结果，也没有打开样本 URL。临时配置未合入生产源码；扫码兼容性仍需独立验证。

结束时停止相机源，还原客户机缓存、消费端动态库和 libcamfix.plist；逐一下载后通过 `cmp` 确认与备份逐字节一致。宿主正常退出 0，socket 已删除。原文件还原后未再次启动复验。保留用户完成的系统设置；本轮没有操作主实例或执行磁盘清理。

A3/E6 均保持进行中。后续项：排除休眠因素后复测应用内返回主屏；验证 QR 解码、有效消费端下的积压恢复、仅重启客户机、视频循环与 EOF。此前无签名产物的消费失败不能继续作为这些项目的有效验收环境。
