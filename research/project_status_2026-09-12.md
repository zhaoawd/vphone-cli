# 项目进展与后续待办（2026-09-12）

核对时间：2026-09-12 15:37–15:40（Asia/Shanghai）。代码与验收基线：`codex/autophone-location-multivm-integration` / `bb9233c`。本次已执行 `git fetch origin --prune`；下述提交差异以该基线计算，不含本次报告提交。

## 整体结果

原清单共 **28 项，14 项完成、14 项未关闭**。完成项为 A1、A2、A4、B1–B4、C1–C3、E1–E4。计数反映工作项状态，不表示功能、工时或产品交付完成百分比。

最近已完成 E2 控制模块分离、E3 headless 控制接入、E4 传输故障与取消处理。E5 已增加定位真实接口证据，但宿主重启、损坏文件启动恢复和双 VM 隔离未验收，因此仍未关闭。C4 按用户决定暂停。

| 范围 | 完成项 / 总项 | 已确认结果 | 未关闭范围与原因 |
| --- | --- | --- | --- |
| A：测试、缓存、触控 | 3 / 4 | 快速测试、AEA 状态识别、GitHub CI 已验收 | A3：底部上滑回主屏有失败记录，原因未查明；缺完整触控组合验收 |
| B：VM 与离线操作保护 | 4 / 4 | 挂载隔离、每 VM 排他锁、停止身份校验、离线占用保护已完成 | B3 部分实机停止场景及 F2 双 VM 全流程仍缺证据，完成范围不能扩展到这些场景 |
| C：固件与补丁 | 3 / 5 | 兼容性清单、结构化结果及必要补丁检查已完成 | C4 完整 less 成功路径受磁盘空间限制；C5 实验记录依赖 C4 |
| D：构建、依赖、创建恢复 | 0 / 5 | 已有构建、资源解析、创建编排等基础代码 | 统一完整包、依赖复现、共同安装逻辑、跨阶段续跑和统一诊断未完成验收 |
| E：自动化控制 | 4 / 6 | E1–E4 已完成；E5 部分真实定位场景通过 | E5 剩余实际运行受宿主执行环境限制；E6 相机回执与应用结果未完成验收 |
| F：系统验收与交付 | 0 / 4 | 已有分项运行记录；文档持续更新 | 端到端、双 VM、性能长时运行和最终交付核对均未完成 |

完整逐项清单见[迭代清单](project_iteration_checklist_2026-09-08.md)。当前具备多项已验证的自动化能力；尚不能据此声明完整分发包、所有固件组合或双 VM 全流程已验收。

## 最近的功能进展

| 工作项 | 已完成内容 | 验证范围与限制 |
| --- | --- | --- |
| E2：控制模块分离 | 分离 socket、命令执行、能力发现和可选画面；180 秒命令期限与协作取消 | 本地完整回归及代码 CI 通过；取消宿主等待不保证客户机操作停止 |
| E3：headless 控制 | 无窗口模式接入公共控制入口；GUI/headless/DFU 能力按实际模式报告；退出清理 socket | 真实入口、客户机连接及退出验证通过；GUI 截图曾为黑屏，未证明客户机界面显示正常 |
| E4：客户机传输 | 4 MiB 帧边界、短读写、请求 ID、并发响应、附带数据、超时、迟到响应、旧连接隔离 | 13 个 Swift socketpair 与 4 个客户机协议用例通过；E4 原 8 GiB rig2 配置后续通过实际启动、断线、自动重连和清理 |
| E5：定位 | 真实入口通过固定位置、流、owner 竞争、generation/sequence、暂停/恢复、停止、超时 hold/stop；客户机重连后保持外部固定源；GUI 宿主同步与外部源切换通过 | 使用原运行宿主与新客户机的精确组合；新构建宿主未能执行。ACK 与宿主 last_fix 不替代应用内 CoreLocation 读数 |
| E5：持久化测试 | 增加恢复 generation 变化断言、实例损坏文件隔离、流替换固定源后不恢复测试 | 65 个定位控制器 XCTest 通过；使用真实状态文件、替身客户机，不替代宿主进程重启或双 VM 验收 |
| C4：固件恢复 | 暂存、哈希、提交记录、显式恢复和待恢复状态保护已有实现；四个非 less 实际场景及 less 中断恢复通过 | 完整 less 成功路径未完成，继续暂停 |
| F4：文档维护 | `aa2405f` 更新 AGENTS.md、vphone600 分析技能及历史文档适用范围 | 属于文档局部进展，F4 整体仍未完成 |

证据：[E2](host_control_e2_2026-09-12.md)、[E3](host_control_e3_2026-09-12.md)、[E4](guest_transport_e4_2026-09-12.md)、[E5](location_e5_2026-09-12.md)、[rig2 排查](rig2_startup_diagnosis_2026-09-12.md)、[C3](c3_completion_acceptance_2026-09-10.md)、[C4](firmware_recovery_c4_2026-09-11.md)。

## 分支与集成

| 分支或提交 | 当前结果 | 后续处理与原因 |
| --- | --- | --- |
| 当前 `codex/autophone-location-multivm-integration` | 基线 `bb9233c`；比对应远端领先 2 个提交，无远端独有提交 | 未推送的是 `aa2405f` 文档与技能更新、`bb9233c` E5 测试和记录；不能把旧 CI 结果归给这两个提交 |
| 对应 origin 工作分支 | `35bb48a` | 已包含 E2–E4 代码及 rig2 后续排查记录 |
| `origin/main` | `06370de`，是当前基线祖先；当前基线领先 52 个提交 | 近期工作尚未进入主线，后续按交付范围评审集成 |
| `origin/autophone-location-multivm-integration`（无 codex 前缀） | 同为 `06370de` | 与当前 codex 工作分支不是同一个远端分支，不能作为最新工作结果 |
| 本地 `main` | `d163eed`，落后 origin/main 90 个提交，无本地独有提交 | 使用前再同步，本次未移动 main |
| 本地 `codex/app-knowledge-compiler-fixes` | `fb57473`；基础定位功能及两项后续修复已按行为整合 | Git 提交图不同不代表功能仍缺失，见[定位整合记录](location_fixes_integration_2026-09-11.md) |
| 本地 `codex/location-capability-consolidation` | `0fc1173`，提交不在当前祖先链 | 仍需按行为审计剩余差异；本次只核对引用，不把其独有提交认定为未实现功能 |
| 本地 `codex/vphone-core-integration`、`codex/upstream-c9ad3c7-integration` | 两个本地分支头均为当前基线祖先 | 不是待合入的本地独有提交；此前 signcert 路径差异也已有等价实现核对 |

远端分支现状发生变化：本次同步确认旧 app-knowledge、location-capability、vphone-core、upstream integration 等多个 origin 分支已删除。`fetch --prune` 仅清理对应远程跟踪引用，本地分支保留。工作区既有 `Makefile` 修改与 `research/markdown_preview.md` 保留。

## 测试与 CI

| 验证 | 结果 | 可支持的结论 |
| --- | --- | --- |
| E4 代码提交 `e7737c3` 的 GitHub CI | 本次重新查询为 completed / success | E4 精确提交的托管检查通过；[运行记录](https://github.com/zhaoawd/vphone-cli/actions/runs/34678764888) |
| E2 / E3 代码 CI | 既有验收记录分别为 `98fe7ec` / `73d6040` success | [E2 CI](https://github.com/zhaoawd/vphone-cli/actions/runs/34675729174)、[E3 CI](https://github.com/zhaoawd/vphone-cli/actions/runs/34677374765)；本次未重复运行 |
| E5 新增测试前 `make test` | 退出 0；85 个 Python、127 个 XCTest（其中 3 个跳过）；Swift Testing 快速套件通过 | 当前源码的无固件回归通过，不证明真实 VM 运行通过 |
| E5 新增测试后定位专项 | 65 个 XCTest，通过 | 覆盖新断言及定位控制器回归；65 项不是在 127 项之外新增的测试数量 |
| E5 宿主和客户机构建 | 构建与签名完成；宿主执行被 AMFI 拒绝 | 静态签名校验成功不保证具备执行条件 |
| E5 最新本地提交 | 未推送 | 暂无该提交的 GitHub CI 结果 |

## 当前环境与限制

| 问题 | 现象与证据 | 原因或影响 |
| --- | --- | --- |
| 新宿主程序无法执行 | E4/E5 app 的 `--help` 返回 -9；AMFI Code=-424 明确拒绝临时签名且带受限 entitlements 的程序 | 本轮执行失败原因已确认。阻断当前构建的 rig2 启动、宿主重启和双 VM 实验；项目 amfidont 脚本尚未获本轮答复，未执行 |
| 历史 rig2 启动超时 | 曾在 120/300 秒内没有客户机连接；后续同配置成功，自动重连约 3.116 秒 | 历史超时原因未查明；不能用本轮 AMFI 的启动前拒绝解释客户机握手超时 |
| 原 VM 当前能力 | 15:37 只读检查：PID 57388 仍运行，guest_connected=true，location_owned=false | E5 测试后已恢复旧守护进程；再次使用新定位源命令需要受控部署新客户机，不能把测试时的能力当成现状 |
| rig2 当前状态 | 控制 socket 不存在；上轮测试已退出 | 没有正在运行的本任务 rig2 验收实例 |
| 磁盘空间 | 本次 `df -k` 可用 13,563,312 KiB，约 **12.94 GiB** | C4 历史预算估计至少约 50 GiB、建议约 60 GiB，峰值未实测。现有空间不足，用户暂停决定保持有效 |
| autophone 共用 VM | E5 前曾确认无任务占用，测试后已释放设备锁并恢复旧文件及定位 off 状态 | 该确认只适用于当时；下次变更 VM 前必须重新检查运行中任务和设备占用 |

本次进度更新只读取 VM 能力，没有重启 VM、更新客户机、调整宿主安全设置或恢复 C4。

## 后续工作与原因

| 优先顺序 | 工作项 | 具体下一步 | 原因与前置条件 |
| --- | --- | --- | --- |
| 当前优先 | E5 | 恢复可执行测试环境；当前 E5 app 重跑真实入口；验证固定源跨宿主重启、新 generation、流不恢复、损坏文件启动及双 VM 隔离 | 已完成主要单实例控制场景，剩余验收范围明确；宿主签名放行仍待具体动作授权 |
| 可先推进 | D1 → D2 | 统一完整包构建与资源检查，补仓库外资源解析；再固定依赖并验证初始化、损坏和回退 | 不依赖 C4，可先做静态检查和无 VM 测试；完整签名应用的执行检查仍受 AMFI 限制 |
| 随后 | E6 | 明确回执是任意帧还是指定帧消费；验证 generation 切换、旧帧、停止、客户机重启，再查应用显示与 QR 结果 | 传输回执不能替代应用结果；代码契约审计可先做，最终验收需要可用 VM |
| 有测试 VM 时 | A3 | 复现并定位底部上滑失败，补触控版本和连接组合 | 已有失败证据，原因未查明，尚未满足交互验收 |
| 可独立准备 | D3 | 提取共用 CFW 步骤，保持变体顺序；比较调用、重复执行和失败清理 | A2/B1 已完成；最终安装结果比较需要样本与空间 |
| 继续暂停 | C4 | 补完整 less 成功路径及恢复验收 | 用户选择暂停，磁盘预算仍不满足 |
| 依赖完成后 | C5 → D4 → D5 | 统一补丁实验记录；结合 D3 实现创建检查点和续跑；结合 D2 输出统一诊断 | 分别依赖 C4、安装阶段约定及稳定状态/依赖模型，不能仅补日志就标记完成 |
| 功能验收后 | F1、F2 | 固定组合端到端与两 VM 同时工作的完整流程 | F1 依赖 A3/C3/D4/E5/E6；F2 依赖 B4/E3–E6 |
| 最后验收 | F3、F4 | 性能和长时运行测量；核对资源、支持范围、文档及交付记录 | F3 需要 F1/F2 的稳定基线；F4 可持续维护，最终状态依赖各项证据 |

未关闭项完整列表：**A3、C4、C5、D1–D5、E5、E6、F1–F4**。若宿主执行环境暂不调整，建议先推进 D1 的构建统一和完整包检查，同时保留 E5 的实机验收缺口。
