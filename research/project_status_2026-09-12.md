# 项目进展与后续待办（2026-09-12）

核对基线：`codex/autophone-location-multivm-integration`，`0eaec95`。本次执行了远端引用更新、分支比较、CI 查询和研究记录核对；没有重新运行构建、测试或真实 VM。

## 整体结果

原清单共 28 项，已完成 **11 项**：A1、A2、A4、B1–B4、C1–C3、E1；未完成 **17 项**。该数字是工作项计数，不表示功能完成百分比。E2 尚未开始实现。C4 按用户决定继续暂停。

E1 对应的 [GitHub CI](https://github.com/zhaoawd/vphone-cli/actions/runs/34572983931) 已完成且结果为 success，提交为 `0eaec9540b97d20e71bf508014baa32698c706ec`。此前“远端 CI 尚未确认”的状态已更新。

## 分支与集成

| 分支 | 当前结果 | 后续处理及原因 |
| --- | --- | --- |
| `codex/autophone-location-multivm-integration` | HEAD 为 `0eaec95`，与对应 origin 分支一致 | 继续作为工作基线 |
| `origin/main` | `06370de`，是当前 HEAD 的祖先；当前分支领先 41 个提交 | 尚未合入近期工作；合并前需明确交付范围和真实运行限制 |
| 本地 `main` | `d163eed`，落后 origin/main 90 个提交 | 使用时再同步，不能作为最新功能依据 |
| `codex/app-knowledge-compiler-fixes` | `fb57473`；两项修复行为和测试已通过 `9682b65` 整合 | 不再列为待移植；E5 的真实运行验收仍未完成 |
| `origin/codex/vphone-core-integration` | signcert 路径差异已核对，当前代码已具备相同行为 | 无需为该差异再次合并 |
| `codex/location-capability-consolidation` | `0fc1173`；原报告中的独有差异尚无后续审计记录 | 仍需按行为核对，不能仅凭提交不同认定功能缺失 |

分支比较均以本次文档更新前的代码基线为准。工作区原有 `Makefile` 修改与 `research/markdown_preview.md` 保留。

## 具体功能进展

| 功能 | 已实现或已验证 | 未完成范围及原因 |
| --- | --- | --- |
| 测试与 CI：A1、A4 | 统一快速测试；GitHub 冷缓存、正常 PR、预期失败、恢复成功、日志和缓存验收完成 | 专用固件运行器及真实 VM 验收未由快速 CI 覆盖 |
| 缓存和离线操作：A2、B1–B4 | AEA 缓存校验、挂载隔离、每 VM 锁、停止身份校验、离线占用保护完成 | 部分停止运行场景及双 VM 全流程仍缺证据 |
| 补丁：C1–C3 | 兼容性清单、结构化结果、必要项判定完成；24 个非 less 场景和 1 个默认 less 场景通过限定产物验收 | 产物通过不能代替恢复、启动及应用功能通过 |
| 固件事务：C4 | 暂存、摘要、提交记录、显式恢复实现；四个非 less 真实场景和 less 中断恢复通过 | 完整 less 成功路径未完成，因磁盘空间限制暂停 |
| 宿主控制：E1 | 2 MiB JSON、1 MiB 内联文件、64 MiB 宿主文件限制；5 秒读写期限；16 个并发连接；同用户权限检查 | 读写期限不覆盖命令执行；截图等回调没有统一取消机制，可能持续占用连接名额 |
| 无 GUI 控制：E2–E4 | 已有 Unix socket 与客户机 vsock 控制实现 | socket 生命周期仍在 GUI 分支；尚未完成能力分离、headless 和故障/取消契约验收 |
| 定位：E5 | 固定源、流、所有权、序号、重连和持久化；9 月 11 日修复已整合 | 真实控制入口、重启、损坏状态和双 VM 隔离尚待验收 |
| 相机：E6 | 图像/视频输入、独立传输、generation 回执已有实现 | 需确定回执表示任意帧还是指定帧消费，并验证应用显示和 QR 识别 |
| 触控：A3 | 点按、拖动、顶部手势有运行记录 | 底部上滑回主屏失败，原因未查明；版本和连接矩阵未齐 |
| 构建、创建、多 VM：D/F | 构建签名、创建编排、VM 库及每 VM 状态已有实现 | 完整分发包、依赖固定、跨阶段续跑、端到端与双 VM 系统验收未完成 |

证据：[E1 协议与验证](host_control_protocol_e1_2026-09-11.md)、[A4 CI](pr_checks_a4_2026-09-11.md)、[C3 验收](c3_completion_acceptance_2026-09-10.md)、[C4 恢复](firmware_recovery_c4_2026-09-11.md)、[定位整合](location_fixes_integration_2026-09-11.md)。

## 验证与环境限制

9 月 11 日 E1 本地验证：81 个 Python 测试通过；92 个 XCTest 中跳过 3 个，其余通过；Swift Testing 331 个测试、53 个 suite 通过。新增 12 个协议测试通过。`make build` 完成 release 编译、签名和 app bundle。本次只核对记录，不将这些结果写成 9 月 12 日重新测试。

本次 `df -k` 显示可用约 **31.1 GiB**。C4 恢复预算此前估计为至少约 50 GiB、建议 60 GiB 可用空间；完整成功路径峰值未实测，因此不视为保证阈值。继续暂停 C4，不创建大型固件镜像。

当前宿主此前拒绝执行带私有 entitlements 的正式 CLI，日志记录签名校验失败；静态签名通过与空 entitlements 临时副本运行通过不能替代正式 CLI/VM 验收。本次没有修改宿主安全设置或复测该限制。

## 后续待办与顺序

| 顺序 | 工作项 | 具体结果与验收要求 | 原因或前置条件 |
| --- | --- | --- | --- |
| 1 | E2 | 分离 socket、命令和可选画面能力；保持命令字段及默认行为；新增能力发现；用替身覆盖 shell、文件、应用、定位、相机和截图；明确命令超时/取消及迟到回调处理 | E1 已完成；E3、E4 依赖该边界；主要实现与测试不需要大型固件 |
| 2 | E3、E4 | E3 将 socket 生命周期移出 GUI 分支，明确 GUI/headless/DFU 能力；E4 验证短读写、4 MiB 边界、ID、断线重连、迟到响应及取消 | 依赖 E2；调用方超时不能直接解释为客户机操作停止；真实运行部分仍需可用宿主环境 |
| 3 | E5、E6 | 定位通过真实入口验证重启和隔离；相机定义回执语义，分别验证客户机消费、应用显示与识别 | 依赖 E3、E4 和真实 VM，不能用控制器单元测试替代 |
| 可独立安排 | D1 → D2 | 统一构建和完整包资源检查，验证仓库外启动；固定实际依赖解析，验证初始化、损坏与回退 | 不依赖 C4；正式包运行验证需解决或换用满足要求的宿主环境 |
| 可独立安排 | D3 | 提取 CFW 公共步骤，保留变体显式顺序；比较迁移前后调用和产物，验证失败清理 | A2、B1 已完成；实际安装验收仍需样本和空间 |
| 有可用测试 VM 时 | A3 | 补齐触控连接/版本矩阵；调查底部上滑回主屏失败 | 已有失败现象，原因未查明 |
| 暂停 | C4 | 完整 less 成功路径、正式 CLI 运行及最终恢复验收 | 用户决定暂停；磁盘与宿主运行条件未满足 |
| C4 完成后 | C5、D4、D5 | C5 统一实验记录；D4 增加跨阶段检查点/续跑；D5 汇总只读诊断 | C5 依赖 C1–C4；D4 依赖 B4、C4、D3；D5 依赖 C5、D2、D4 等状态基础 |
| 前置验收完成后 | F1、F2 | 固定组合端到端与两 VM 并行验收，记录恢复、两次启动、应用、定位、相机和停止隔离 | F1 依赖 A3、C3、D4、E5、E6；F2 依赖 B4、E3–E6 |
| 最后 | F3、F4 | 固定条件测量延迟、资源和长时运行；核对模块、支持范围、资源与交付记录 | F3 需要 F1、F2 的稳定运行基础；F4 随各项完成持续更新并最终核对 |

17 个未完成项：A3、C4、C5、D1–D5、E2–E6、F1–F4。下一次代码工作优先 **E2**。本次仅更新进度，不将后续计划记为已开始或已完成。
