# E5 定位验收准备

日期：2026-09-14。已完成离线输入核对与剩余场景整理，未进行新的真实定位验收。用户说明其他任务正在使用 VM；本轮不取得设备锁、不查询控制 socket、不启停 VM、不更新客户机、不写定位持久文件。

## 离线核对

16:55 的只读记录保存于 `research/artifacts/e5-preparation-2026-09-14/offline-inventory.json`，同目录保留生成脚本。

| 实例 | 已确认的宿主文件 | 配置 | 未确认条件 |
| --- | --- | --- | --- |
| `vm-2607` | config、Disk.img、NVRAM、SEPStorage 均存在；控制 socket 路径存在 | 8 CPU、8 GiB | 实际占用及客户机能力未查询；socket 存在不等于接口可用 |
| `vm-2607-rig2` | 上述四类文件存在；控制 socket 路径不存在 | 8 CPU、8 GiB | 未判定是否可独占，也未判定可启动 |
| `vm-c3-runtime-20260914` | 上述四类文件存在；无 `.vphoned.signed` 缓存 | 8 CPU、8 GiB | less 运行证据不证明 `location_owned` 能力可用 |

三份配置的 machineIdentifier 摘要不同，配置和客户机缓存摘要已保存。三实例均未见 `system-location.json`；这些是读取时的文件状态，不作为以后执行时的初始状态。

## 执行前条件

- 与使用 VM 的任务协调时间，重新确认目标实例及设备别名占用。没有 socket 不能替代占用检查。
- 为本轮代码生成独立签名宿主与客户机，记录代码状态、两个二进制摘要、固件构建号、实例身份和启动参数。用该宿主验证只读命令及 preflight；不沿用旧宿主的执行成功记录。
- 启动后通过 `capabilities` 确认客户机已连接并提供 `location_owned`。旧缓存的哈希相同或不同都不能替代能力查询。
- 在专用实例上备份定位状态和客户机部署状态，明确恢复方式。损坏文件实验只能操作已确认独占的测试实例。
- 优先评估 rig2 的单实例验收；双实例使用哪两个 VM 在占用协调后确定。不得直接重跑绑定旧 PID、旧路径及旧缓存的历史脚本。

## 剩余场景与判据

| 顺序 | 场景 | 操作与判据 | 保留证据 |
| --- | --- | --- | --- |
| 1 | 当前构建的真实入口基线 | 重跑固定位置、流、owner 冲突、序列、暂停、停止及超时场景；记录实际客户机能力 | 请求/响应、宿主与客户机摘要、实例身份 |
| 2 | 固定位置跨宿主程序重启 | 设置 `persist:true`，保存 generation 和固定位置；有序停止并重启同一测试实例，确认固定位置恢复且 generation 改变 | 重启前后状态、持久文件摘要、启动日志 |
| 3 | 流不恢复 | 用流替换固定源，确认持久文件清除；重启后为 off，不恢复旧固定源或流 | 替换、重启前后状态与文件存在性 |
| 4 | 损坏文件启动 | 仅在关闭且独占的测试实例写入截断 JSON；启动后确认 `location_persistence_corrupt`、状态 off，原文件隔离为 `.corrupt-*` | 输入摘要、隔离文件、状态响应与日志 |
| 5 | 双 VM 隔离 | 两实例使用不同 owner 与坐标，确认 generation、持久文件和交付状态各自独立；停止或重启一个时另一个仍正常 | 两个控制入口逐次响应、文件摘要、清理结果 |

以上重启指宿主 VM 进程的停止与重新启动，不要求重启物理 Mac。每次停止只作用于经过身份核对的目标实例。协议 ACK 与宿主 `last_fix` 不能代替应用内 CoreLocation 读数；应用读数另记范围与证据。

E5 不关闭；本轮也不将离线准备计作 F2 双 VM 验收。已完成的历史范围见 [E5 真实入口记录](location_e5_2026-09-12.md)。

## 2026-09-15 执行前复核

- 当前提交为 `bc7f075b383369dfa274d89ea5e564be34c0377a`。定位控制器、适配器、菜单、提供者及三份定位测试与 `.build/offline-next/source` 隔离副本逐字节一致。
- 隔离签名宿主 `--help` 返回 0。本次仅证明该程序当前可执行帮助命令，不证明 VM 能启动或客户机具有定位能力。
- 使用隔离副本已有构建执行三个定位专项测试类：70 项通过、0 失败。测试使用临时状态与替身客户机，不替代真实重启和双 VM 验收。
- 宿主与客户机摘要及测试日志保存在 `research/artifacts/e5-location-2026-09-15/`，分别为 `offline-inputs.json`、`host-help.log` 和 `location-tests.log`。
- 用户此前说明其他任务正在使用 VM。已询问本轮可独占的实例，尚未收到实例使用范围；本轮未查询控制 socket、取得设备锁、部署客户机、改变定位状态或启停 VM。

下一步在明确可用实例后，先核对占用与初始状态，再执行真实入口基线、固定源重启恢复、流不恢复和损坏文件场景；双 VM 隔离需要两个可独占实例。E5 仍为进行中。

用户随后指定 rig2，已完成单实例实际验收及恢复错误修复，见 [2026-09-15 运行记录](location_e5_rig2_2026-09-15.md)。上述等待实例选择的状态仅为此前记录；目前 rig2 已关闭、客户机缓存已恢复，E5 剩余双 VM 隔离。
